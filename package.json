{
  "name": "local-browser-ai",
  "version": "2.2.0",
  "description": "A simple Browser extension for using the Prompt API.",
  "main": "background.js",
  "scripts": {
    "update-vendors": "node update-vendors.js & node update-icons.js",
    "postinstall": "npm run update-vendors.js",
    "fmt": "prettier --write .",
    "build": "npm t & node build.js",
    "test": "node --test",
    "preversion": "npm t"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "type": "module",
  "devDependencies": {
    "archiver": "^7.0.1",
    "esbuild": "^0.25.10",
    "prettier": "^3.6.2"
  },
  "dependencies": {
    "@mozilla/readability": "^0.6.0",
    "markdown-it": "^14.1.0",
    "turndown": "^7.2.1",
    "turndown-plugin-gfm": "^1.0.2"
  },
  "_vendor": {
    "markdown-it": "{ default as MarkdownIt }",
    "turndown": {
      "what": "{ default as TurndownService }"
    },
    "@mozilla/readability": {
      "what": "{ Readability }"
    }
  }
}
